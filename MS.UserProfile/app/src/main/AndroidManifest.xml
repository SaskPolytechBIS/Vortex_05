<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MSUserProfile"
        tools:targetApi="31">

        <!-- LoginActivity: The launcher screen -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Registration Screen -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />

        <!-- Consent Screen -->
        <activity
            android:name=".ConsentActivity"
            android:exported="true" />

        <!-- User Profile Screen -->
        <activity
            android:name=".ProfileActivity"
            android:exported="true" />

        <!-- Game Menu Screen -->
        <activity
            android:name=".GameMenuActivity"
            android:exported="true" />

        <!-- NeuroCycle Game Screen -->
        <activity
            android:name=".NeuroCycleGameActivity"
            android:exported="true" /> <!-- This is the new activity you added -->

    </application>

</manifest>
